# Instalar después de PyTorch (ver scripts/setup_env_windows.ps1).
# Python 3.11 o 3.12 recomendado.

transformers>=4.40.0
datasets>=2.18.0
peft>=0.11.0
trl>=0.9.0
accelerate>=0.30.0
sentencepiece
huggingface_hub
tqdm
jsonlines
pandas
requests
beautifulsoup4
html2text
pypdf>=4.0.0
google-genai

# bitsandbytes: útil con GPU NVIDIA + CUDA en Windows; en CPU no hace falta.
# Si pip falla, omítelo y usa entrenamiento float32/CPU o otra máquina con CUDA.

# Opcionales
# wandb
# evaluate
# rouge_score
